type TokenType {
    success: String
}

type PasswordResetTokenType {
    userId: String
}

extend type Query {
    confirmAccount(token: String): TokenType!
    confirmPasswordReset(token: String): PasswordResetTokenType!
}
