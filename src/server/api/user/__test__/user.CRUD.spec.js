const { findUser, createNewUser } = require('../user.CRUD');

// mongodb://admin:oaktree99@ds237588.mlab.com:37588/progress-tracker-test-env

describe('user.CRUD', () => {
    describe('@findUser', () => {
        test('should ', () => {
        })
    })

    describe('@createNewUser', () => {
        test('should merge', () => {
        })
    })
});
